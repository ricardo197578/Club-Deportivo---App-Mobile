<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Design.Light.NoActionBar">

        <!-- ========================= -->
        <!--  ADMIN – PAGOS / REPORTES -->
        <!-- ========================= -->
        <activity android:name=".admin.PagoCuotaActivity" />
        <activity android:name=".admin.PaseDiarioActivity" />
        <activity android:name=".admin.PagosDelDiaActivity" />
        <activity android:name=".admin.CuotasVencidasActivity" />
        <activity android:name=".admin.ReporteDetalleActivity"
            android:exported="false" />
        <activity android:name=".admin.ReportesActivity"
            android:exported="false" />

        <!-- ========================= -->
        <!--      CRUD ENTIDADES       -->
        <!-- ========================= -->
        <activity android:name=".admin.ActividadFormActivity"
            android:exported="false" />
        <activity android:name=".admin.ActividadesListActivity"
            android:exported="false" />

        <activity android:name=".admin.NoSocioFormActivity"
            android:exported="false" />
        <activity android:name=".admin.NoSociosListActivity"
            android:exported="false" />

        <activity android:name=".admin.SocioFormActivity"
            android:exported="false" />
        <activity android:name=".admin.SociosListActivity"
            android:exported="false" />

        <!-- ========================= -->
        <!--     HOME SCREENS          -->
        <!-- ========================= -->
        <activity android:name=".home.HomeNoSocioActivity"
            android:exported="false" />
        <activity android:name=".home.HomeSocioActivity"
            android:exported="false" />

        <!-- ✔ ESTA ERA LA QUE FALTABA -->
        <activity android:name=".home.HomeAdminActivity"
            android:exported="false" />

        <!-- ========================= -->
        <!--  CORE APP (LOGIN / MAIN)  -->
        <!-- ========================= -->
        <activity android:name=".RegisterActivity"
            android:exported="false" />
        <activity android:name=".DashboardActivity"
            android:exported="false" />
        <activity android:name=".LoginActivity"
            android:exported="false" />

        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
